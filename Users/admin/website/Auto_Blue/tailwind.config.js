/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,js,ts,jsx,tsx}",  // Adjust this to match your project's file paths
  ],
  theme: {
    extend: {
      // You can add custom colors, gradients, etc. here if needed
    },
  },
  plugins: [],
};